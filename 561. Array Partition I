class Solution:
    def arrayPairSum(self, nums: List[int]) -> int:
        nums.sort()
        total = 0
        
        for index in range( 0, len(nums), 2):
            total += nums[index]
        
        return total
